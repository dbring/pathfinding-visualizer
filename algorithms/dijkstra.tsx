import { Node } from "../types/types";

export const dijkstra = (
  numRows: number,
  numCols: number,
  startNode: Node,
  targetNode: Node
) => {
  console.log(numRows, numCols, startNode, targetNode);
};
